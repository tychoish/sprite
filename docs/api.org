#+TITLE: Sprite API Reference
#+OPTIONS: toc:3 num:nil

API documentation for the entire =sprite= package, generated from docstrings.

* API Reference
#+BEGIN_SRC emacs-lisp :results output raw :exports none
  (require 'org-docsgen)
  (let ((base (expand-file-name ".." default-directory)))
    (org-docsgen-run
     :el-files
     (delq nil
           (mapcar (lambda (f) (expand-file-name f base))
                   '("sprite.el"
                     "sprite-direct.el"
                     "sprite-fleet.el"
                     "sprite-future.el"
                     "sprite-heartbeat.el"
                     "sprite-list.el"
                     "sprite-session.el")))
     :scope 'exported
     :include-kinds '(variables customs)
     :namespace "sprite"
     :section-style 'triple-semi
     :link-style 'org
     :group-by '("sprite"
                 "sprite-direct"
                 "sprite-fleet"
                 "sprite-future"
                 "sprite-heartbeat"
                 "sprite-list"
                 "sprite-session")))
#+END_SRC

#+RESULTS:
** sprite

**** TODO [[file:../sprite.el::55][server-use-tcp]] :variable:
:PROPERTIES:
:CUSTOM_ID: server-use-tcp
:END:

*no docstring*

**** TODO [[file:../sprite.el::56][server-auth-dir]] :variable:
:PROPERTIES:
:CUSTOM_ID: server-auth-dir
:END:

*no docstring*

**** [[file:../sprite.el::66][sprite-instance-id]] :variable:
:PROPERTIES:
:CUSTOM_ID: sprite-instance-id
:END:

Name of the running Emacs instance. Set by ‘sprite-set-up-instance-name’ at startup.

**** [[file:../sprite.el::73][sprite-cli-instance-id]] :variable:
:PROPERTIES:
:CUSTOM_ID: sprite-cli-instance-id
:END:

CLI-specified instance name; set from --id command-line arguments.

**** [[file:../sprite.el::76][sprite-resolve-instance-id]]
:PROPERTIES:
:CUSTOM_ID: sprite-resolve-instance-id
:END:

Return the current Emacs instance ID. Resolution order: daemon name, ~sprite-cli-instance-id~, ~sprite-instance-id~, then "solo".

**** [[file:../sprite.el::91][sprite-instance-name]]
:PROPERTIES:
:CUSTOM_ID: sprite-instance-name
:END:

Return the current Emacs instance name, initialising it if needed. Caches the result in ~sprite-instance-id~.  This is the preferred public accessor; call ~sprite-resolve-instance-id~ only when the raw resolution chain must be re-evaluated.

**** TODO [[file:../sprite.el::100][sprite-system-name]]
:PROPERTIES:
:CUSTOM_ID: sprite-system-name
:END:

*no docstring*

**** [[file:../sprite.el::103][sprite-conf-host-and-instance]]
:PROPERTIES:
:CUSTOM_ID: sprite-conf-host-and-instance
:END:

Return (HOSTNAME INSTANCE-ID) for state-path construction.

**** [[file:../sprite.el::110][sprite-state-file-prefix]]
:PROPERTIES:
:CUSTOM_ID: sprite-state-file-prefix
:END:

Return the instance-scoped filename prefix for NAME. Produces HOSTNAME-INSTANCE-NAME[-USERNAME] where USERNAME is included only when running as root or under a symlinked ~user-emacs-directory~.

**** [[file:../sprite.el::126][sprite-state-path]]
:PROPERTIES:
:CUSTOM_ID: sprite-state-path
:END:

Return the full state-directory path for NAME, scoped to this host and instance.

**** [[file:../sprite.el::135][sprite-cli-resolve-id]]
:PROPERTIES:
:CUSTOM_ID: sprite-cli-resolve-id
:END:

Parse --id=NAME or --id NAME from ~command-line-functions~ context. Sets ~sprite-cli-instance-id~ when found.

**** [[file:../sprite.el::190][sprite-state-directory]]
:PROPERTIES:
:CUSTOM_ID: sprite-state-directory
:END:

: (fn &key FULL-NAME)

Return the state directory for sprite instance /FULL-NAME/, or the sprite root if nil.

**** [[file:../sprite.el::197][sprite-conf-state-path]]
:PROPERTIES:
:CUSTOM_ID: sprite-conf-state-path
:END:

: (fn NAME &key FULL-NAME)

Return path for /NAME/ within /FULL-NAME/’s state directory. If /FULL-NAME/ is nil, returns path under the sprite root.

**** [[file:../sprite.el::355][sprite-define]]
:PROPERTIES:
:CUSTOM_ID: sprite-define
:END:

: (fn &key UNIQUE-NAME (PARENT (sprite-instance-name)) (STARTUP 'idle))

Declare that a sprite named /UNIQUE-NAME/ should exist under PARENT. STARTUP controls creation when absent: ‘sync’  — create immediately when ~sprite-defs-activate~ runs. ‘idle’  — create on the next Emacs idle period after activation. Definitions persist across sessions via savehist.  Returns the provisional sprite struct.

**** [[file:../sprite.el::371][sprite-defs-activate]]
:PROPERTIES:
:CUSTOM_ID: sprite-defs-activate
:END:

Reconcile declared definitions with the running instance. For each provisional entry whose :parent matches ~sprite-instance-name~: - If a real sprite with the same (parent, unique-name) is already in the registry, promote it (copy startup, remove provisional entry). - If absent and startup is ‘sync’, create the sprite immediately. - If absent and startup is ‘idle’, schedule via the session idle hook.

**** [[file:../sprite.el::400][sprite-defs-activate-idle-check]]
:PROPERTIES:
:CUSTOM_ID: sprite-defs-activate-idle-check
:END:

Idle hook: activate pending definitions; deregister when none remain.

**** [[file:../sprite.el::456][with-sprite]]
:PROPERTIES:
:CUSTOM_ID: with-sprite
:END:

: (fn NAME FORM &key NO-LOG)

Evaluate /FORM/ in sprite /NAME/ via emacsclient. Returns the read result, or nil if the connection fails. Unless /NO-LOG/ is non-nil, logs the exchange and updates last-contact.

**** [[file:../sprite.el::479][sprite-open-log]] :command:
:PROPERTIES:
:CUSTOM_ID: sprite-open-log
:END:

Switch to the communication log buffer for sprite NAME.

*** Lifecycle

**** [[file:../sprite.el::547][sprite-create]] :command:
:PROPERTIES:
:CUSTOM_ID: sprite-create
:END:

: (fn UNIQUE-NAME &key TIMEOUT)

Spawn a new sprite daemon with /UNIQUE-NAME/ under the current instance. The current instance’s ID becomes the parent. Returns the new sprite struct.

Times out after /TIMEOUT/ seconds.

**** [[file:../sprite.el::594][sprite-stop]] :command:
:PROPERTIES:
:CUSTOM_ID: sprite-stop
:END:

Send (kill-emacs) to sprite NAME via emacsclient.

**** [[file:../sprite.el::598][sprite-restart]] :command:
:PROPERTIES:
:CUSTOM_ID: sprite-restart
:END:

Restart sprite NAME: stop it then create a new daemon with the same unique-name.

**** [[file:../sprite.el::611][sprite-decommission]] :command:
:PROPERTIES:
:CUSTOM_ID: sprite-decommission
:END:

Decommission sprite NAME: stop it and write the DECOMMISSIONED marker.

**** [[file:../sprite.el::624][sprite-communication-backend]] :option:
:PROPERTIES:
:CUSTOM_ID: sprite-communication-backend
:END:

Transport used for sprite eval calls. ‘emacsclient’ shells out to the emacsclient binary (default, matches current behaviour).  ‘direct’ speaks the server wire protocol over a socket via sprite-direct.el, with no subprocess.

**** [[file:../sprite.el::632][sprite-communication-fallback]] :option:
:PROPERTIES:
:CUSTOM_ID: sprite-communication-fallback
:END:

When non-nil, retry with the other backend after a failed direct call. Only applies when ~sprite-communication-backend~ is ‘direct’: a failed socket connection retries once via emacsclient before giving up.

**** [[file:../sprite.el::676][sprite-open-frame]] :command:
:PROPERTIES:
:CUSTOM_ID: sprite-open-frame
:END:

Open a new Emacs frame connected to SPRITE. When called interactively, select from accessible sprites via ~annotated-completing-read~ (when available), annotated with status and uptime.

*** Fleet Api

**** [[file:../sprite.el::707][sprite-active-threshold]] :option:
:PROPERTIES:
:CUSTOM_ID: sprite-active-threshold
:END:

Seconds since last-contact after which a sprite is considered available. A sprite is "active" if it was contacted within this many seconds.

**** [[file:../sprite.el::713][sprite-max-count]] :option:
:PROPERTIES:
:CUSTOM_ID: sprite-max-count
:END:

Maximum number of sprite ~sprite-get-or-create-next~ will spawn.

**** [[file:../sprite.el::718][sprite-startup-timeout]] :option:
:PROPERTIES:
:CUSTOM_ID: sprite-startup-timeout
:END:

Seconds to wait for a newly spawned sprite server to accept connections.

**** [[file:../sprite.el::724][sprite-worker-p]]
:PROPERTIES:
:CUSTOM_ID: sprite-worker-p
:END:

Return t if this instance is itself a sprite (has a parent).

**** [[file:../sprite.el::728][sprite-resolve-list]]
:PROPERTIES:
:CUSTOM_ID: sprite-resolve-list
:END:

Return list of sprite structs accessible to this instance. Includes direct children.  If this instance is itself a sprite (worker), also includes sibling sprite from the same parent.

**** [[file:../sprite.el::745][sprite-controller-p]]
:PROPERTIES:
:CUSTOM_ID: sprite-controller-p
:END:

Return t if this instance has at least one non-decommissioned sprite.

**** [[file:../sprite.el::752][sprite-available-p]]
:PROPERTIES:
:CUSTOM_ID: sprite-available-p
:END:

Return t if any accessible sprite are configured.

**** [[file:../sprite.el::772][sprite-get-next]]
:PROPERTIES:
:CUSTOM_ID: sprite-get-next
:END:

Return the struct of the next available sprite, or nil. "Available" means running and not recently contacted.

**** [[file:../sprite.el::778][sprite-get-or-create-next]]
:PROPERTIES:
:CUSTOM_ID: sprite-get-or-create-next
:END:

: (fn &key TIMEOUT)

Return the next available sprite, creating one if none are free. Times out after /TIMEOUT/ seconds.  Signals ~user-error~ if ~sprite-max-count~ would be exceeded.

**** [[file:../sprite.el::790][sprite-get-or-create-fleet]]
:PROPERTIES:
:CUSTOM_ID: sprite-get-or-create-fleet
:END:

: (fn COUNT &key TIMEOUT)

Return a list of up to /COUNT/ available or newly created sprite structs. Reuses available sprites first, spawning new ones up to ~sprite-max-count~ or /COUNT/.

**** TODO [[file:../sprite.el::813][sprite-mode-command-map]]
:PROPERTIES:
:CUSTOM_ID: sprite-mode-command-map
:END:

*no docstring*

**** [[file:../sprite.el::819][sprite-mode-map-prefix]] :option:
:PROPERTIES:
:CUSTOM_ID: sprite-mode-map-prefix
:END:

Cons storing ~sprite-mode-command-map~ is bound as a nested prefix keymap.

Applied when ~sprite-mode~ is enabled and removed when it is disabled.  Set to nil to skip binding it into any parent keymap.

**** [[file:../sprite.el::874][sprite-mode-map]]
:PROPERTIES:
:CUSTOM_ID: sprite-mode-map
:END:

Empty by design -- ~sprite-mode~ ships no default bindings of its own. See ~sprite-mode-command-map~ for commands, nested per ~sprite-mode-map-prefix~; bind directly into this map for anything that should be active unconditionally whenever ~sprite-mode~ is on.

**** [[file:../sprite.el::881][sprite-mode]] :command:
:PROPERTIES:
:CUSTOM_ID: sprite-mode
:END:

Global minor mode for managing sprite (subordinate Emacs daemon) state.

Enabling wires the registry into ‘savehist’ persistence and scans the filesystem for already-running sprites to populate the registry; disabling removes the ‘savehist’ hooks and variable registration. See ~sprite-mode-map~ and ~sprite-mode-command-map~.

This is a global minor mode.  If called interactively, toggle the ‘Sprite mode’ mode.  If the prefix argument is positive, enable the mode, and if it is zero or negative, disable the mode.

If called from Lisp, toggle the mode if ARG is ‘toggle’.  Enable the mode if ARG is nil, omitted, or is a positive number.  Disable the mode if ARG is a negative number.

To check whether the minor mode is enabled in the current buffer, evaluate ‘(default-value 'sprite-mode)’.

The mode’s hook is called both when the mode is enabled and when it is disabled.

** sprite-direct

**** TODO [[file:../sprite-direct.el::73][server-socket-dir]] :variable:
:PROPERTIES:
:CUSTOM_ID: server-socket-dir
:END:

*no docstring*

**** TODO [[file:../sprite-direct.el::74][server-auth-dir]] :variable:
:PROPERTIES:
:CUSTOM_ID: server-auth-dir
:END:

*no docstring*

**** [[file:../sprite-direct.el::78][sprite-direct-blocking-timeout]] :variable:
:PROPERTIES:
:CUSTOM_ID: sprite-direct-blocking-timeout
:END:

Seconds to wait in blocking receives, or nil to block indefinitely. Dynamically override per call-site with ~let~ to impose a local limit.

**** [[file:../sprite-direct.el::82][sprite-direct-yield-interval]] :variable:
:PROPERTIES:
:CUSTOM_ID: sprite-direct-yield-interval
:END:

Seconds passed to ~accept-process-output~ between generator yields. Smaller values increase responsiveness at the cost of CPU spin.

**** [[file:../sprite-direct.el::86][sprite-direct-async-check-interval]] :variable:
:PROPERTIES:
:CUSTOM_ID: sprite-direct-async-check-interval
:END:

Seconds between event polls in ~sprite-direct-promise-wait~.

*** Network Process

**** [[file:../sprite-direct.el::247][sprite-direct-open]]
:PROPERTIES:
:CUSTOM_ID: sprite-direct-open
:END:

Return a ‘sprite-direct-conn’ for TARGET. TARGET is a Unix socket name (e.g. "work.0.render") or HOST:PORT:KEY. For TCP targets an auth key is required and ~user-error~ is signalled when none is found.  For Unix sockets the auth key is optional: Emacs 29+ authenticates local connections via peer UID, so no cookie file is written.

**** [[file:../sprite-direct.el::259][with-sprite-direct]]
:PROPERTIES:
:CUSTOM_ID: with-sprite-direct
:END:

: (fn (VAR TARGET) &rest BODY)

Bind VAR to a connection context for TARGET and evaluate /BODY/. Each operation in /BODY/ opens a transient socket as needed. TARGET is evaluated once; the conn is created by ~sprite-direct-open~.

**** [[file:../sprite-direct.el::323][sprite-direct-eval-blocking]]
:PROPERTIES:
:CUSTOM_ID: sprite-direct-eval-blocking
:END:

: (fn CONN FORM &key TIMEOUT)

Evaluate /FORM/ in the sprite via /CONN/; return the Lisp value. Opens a transient socket, sends the form, and blocks using the generator receive loop.  /TIMEOUT/ overrides ~sprite-direct-blocking-timeout~. Returns nil on connection failure or server error.

**** [[file:../sprite-direct.el::345][sprite-direct-call-and-read]]
:PROPERTIES:
:CUSTOM_ID: sprite-direct-call-and-read
:END:

: (fn TARGET FORM &key TIMEOUT)

Evaluate /FORM/ in the Emacs server at /TARGET/; return the read Lisp value. Drop-in replacement for ~sprite--call-and-read~ in sprite.el. /TARGET/ is a Unix socket name or HOST:PORT:KEY TCP string. /TIMEOUT/ is passed to ~sprite-direct-eval-blocking~. Returns nil on connection failure, missing auth key, or server error.

**** [[file:../sprite-direct.el::399][sprite-direct-eval-non-blocking]]
:PROPERTIES:
:CUSTOM_ID: sprite-direct-eval-non-blocking
:END:

: (fn CONN FORM &key RESULT-BUFFER)

Evaluate /FORM/ in /CONN/ asynchronously; return a ‘sprite-direct-promise’. The promise resolves when the server closes the connection (via sentinel). /RESULT-BUFFER/ is an optional buffer name; when the promise resolves, the result is appended to it as a ~prin1~ line.  Returns the promise immediately. If the connection cannot be opened, the promise is in :rejected state.

**** [[file:../sprite-direct.el::423][sprite-direct-promise-pending-p]]
:PROPERTIES:
:CUSTOM_ID: sprite-direct-promise-pending-p
:END:

Return t when PROMISE has not yet resolved or been rejected.

**** [[file:../sprite-direct.el::427][sprite-direct-promise-resolved-p]]
:PROPERTIES:
:CUSTOM_ID: sprite-direct-promise-resolved-p
:END:

Return t when PROMISE resolved successfully.

**** [[file:../sprite-direct.el::431][sprite-direct-promise-rejected-p]]
:PROPERTIES:
:CUSTOM_ID: sprite-direct-promise-rejected-p
:END:

Return t when PROMISE failed. Either the connection could not be opened, or it closed without ever producing a ‘-print’/‘-error’ response line.

**** [[file:../sprite-direct.el::437][sprite-direct-promise-wait]]
:PROPERTIES:
:CUSTOM_ID: sprite-direct-promise-wait
:END:

: (fn PROMISE &key TIMEOUT)

Block until /PROMISE/ resolves; return its value or nil on timeout/rejection. Drives process output while waiting.  /TIMEOUT/ overrides ~sprite-direct-blocking-timeout~; nil means wait indefinitely.

**** [[file:../sprite-direct.el::449][sprite-direct-promise-then]]
:PROPERTIES:
:CUSTOM_ID: sprite-direct-promise-then
:END:

Arrange for CALLBACK to run once PROMISE leaves its ~:pending~ state. Unlike ~sprite-direct-promise-wait~, this does not block: it polls PROMISE on a repeating timer (~sprite-direct-async-check-interval~) and cancels itself the first time ~sprite-direct-promise-pending-p~ is nil, at which point it calls CALLBACK with two arguments, STATE (~:resolved~ or ~:rejected~) and VALUE (~sprite-direct-promise-value~, nil when rejected). The promise’s own sentinel still does the actual work of receiving the result; this only supplies the missing "notify me when done" callback on top of the pending/resolved/rejected state it already exposes.  Returns the timer, so the caller can ~cancel-timer~ it to abandon the callback.

**** [[file:../sprite-direct.el::472][sprite-direct-list-buffers]]
:PROPERTIES:
:CUSTOM_ID: sprite-direct-list-buffers
:END:

Return a list of buffer names in the sprite via CONN.

**** [[file:../sprite-direct.el::476][sprite-direct-insert-into-buffer]]
:PROPERTIES:
:CUSTOM_ID: sprite-direct-insert-into-buffer
:END:

: (fn CONN BUF-NAME TEXT &key POSITION)

Insert /TEXT/ into /BUF-NAME/ in the sprite via /CONN/. When /POSITION/ is non-nil, go there first; otherwise insert at ~point-max~.

**** [[file:../sprite-direct.el::486][sprite-direct-with-current-buffer]]
:PROPERTIES:
:CUSTOM_ID: sprite-direct-with-current-buffer
:END:

: (fn (CONN BUF-NAME) &rest BODY)

Evaluate /BODY/ forms in the sprite via CONN with BUF-NAME current. /BODY/ is literal Emacs Lisp sent verbatim to the sprite; local variables are not in scope.  The result of the last /BODY/ form is returned.

**** [[file:../sprite-direct.el::499][sprite-direct-read-buffer]]
:PROPERTIES:
:CUSTOM_ID: sprite-direct-read-buffer
:END:

: (fn CONN BUF-NAME &key START END)

Return the contents of /BUF-NAME/ in the sprite via /CONN/. /START/ and /END/ are buffer positions (defaults: ~point-min~ and ~point-max~). Returns a string, or nil on failure.

**** [[file:../sprite-direct.el::510][sprite-direct-value-of-symbol]]
:PROPERTIES:
:CUSTOM_ID: sprite-direct-value-of-symbol
:END:

Return the value of SYMBOL in the sprite via CONN. Evaluates ‘(symbol-value 'SYMBOL)’ in the sprite; returns nil on failure.

*** Tcp Server File Utility

**** [[file:../sprite-direct.el::517][sprite-direct-read-tcp-server-file]]
:PROPERTIES:
:CUSTOM_ID: sprite-direct-read-tcp-server-file
:END:

Read the TCP server file at PATH; return its HOST:PORT:KEY string. Returns nil when PATH is absent or unreadable.

** sprite-fleet

*** [[file:../sprite-fleet.el::46][sprite-fleet-mapcar-sprites]]
:PROPERTIES:
:CUSTOM_ID: sprite-fleet-mapcar-sprites
:END:

: (fn FORM SPRITES &key TIMEOUT)

Evaluate /FORM/ in each of /SPRITES/ concurrently; return results in order. Mirrors ~mapcar~: one result per input, order preserved.  A sprite that times out or rejects contributes nil at its position.  /SPRITES/ is a list of ‘sprite’ structs, e.g. from ~sprite-resolve-list~.

*** [[file:../sprite-fleet.el::55][sprite-fleet-mapc]]
:PROPERTIES:
:CUSTOM_ID: sprite-fleet-mapc
:END:

Evaluate FORM in each of SPRITES concurrently; discard results. Mirrors ~mapc~: dispatches to every sprite and returns SPRITES immediately without waiting for any of them to settle.  SPRITES is a list of ‘sprite’ structs, e.g. from ~sprite-resolve-list~.

*** [[file:../sprite-fleet.el::64][sprite-fleet-mapcar]]
:PROPERTIES:
:CUSTOM_ID: sprite-fleet-mapcar
:END:

: (fn FN-OR-FORM ITEMS &key FLEET-SIZE SPRITES TIMEOUT ASYNC)

Evaluate /FN-OR-FORM/ for each item in /ITEMS/ using a fleet of sprites. /FN-OR-FORM/ can be a unary function/lambda symbol or a form to evaluate. When /FN-OR-FORM/ is a function symbol or lambda, each item is passed as an arg. When /FN-OR-FORM/ is a form containing ‘item’ or ‘_’, that form is evaluated remotely with the item bound.

/FLEET-SIZE/ is the maximum number of concurrent sprites to use (defaults to ~sprite-max-count~ or the length of /ITEMS/).

/SPRITES/ is an optional list of sprite structs or target name strings to use as the worker fleet.  When omitted, sprites are obtained via ~sprite-get-or-create-fleet~.

/TIMEOUT/ is passed to ~sprite-future-wait~ when operating synchronously.

If /ASYNC/ is non-nil, returns a ‘sprite-future’ immediately that resolves to the ordered list of results when all items complete.  Otherwise, blocks and returns the ordered list of results.

*** [[file:../sprite-fleet.el::123][sprite-fleet-let]]
:PROPERTIES:
:CUSTOM_ID: sprite-fleet-let
:END:

: (fn BINDINGS &rest BODY)

Like ~let~, but each binding’s value comes from a concurrent sprite eval. Each element of /BINDINGS/ is (VAR SPRITE FORM): SPRITE is an expression evaluated to a sprite full-name string, FORM is unquoted and evaluated remotely (quoted automatically, as with ~with-sprite~).  All FORMs are dispatched in parallel; /BODY/ runs once every binding has settled, with VAR bound to its resolved value.

** sprite-future

*** [[file:../sprite-future.el::58][sprite-future-pending-p]]
:PROPERTIES:
:CUSTOM_ID: sprite-future-pending-p
:END:

Return t when FUTURE has not yet resolved or been rejected.

*** [[file:../sprite-future.el::62][sprite-future-resolved-p]]
:PROPERTIES:
:CUSTOM_ID: sprite-future-resolved-p
:END:

Return t when FUTURE resolved successfully.

*** [[file:../sprite-future.el::66][sprite-future-rejected-p]]
:PROPERTIES:
:CUSTOM_ID: sprite-future-rejected-p
:END:

Return t when FUTURE failed. For the ‘direct’ backend this delegates to ~sprite-direct-promise-rejected-p~ via the promise sentinel; for ‘emacsclient’ it reflects a non-zero exit code from the ‘emacsclient’ process.

*** [[file:../sprite-future.el::105][sprite-future-then]]
:PROPERTIES:
:CUSTOM_ID: sprite-future-then
:END:

Chain ON-RESOLVE (and optionally ON-REJECT) onto FUTURE. Returns a new ‘sprite-future’ that settles once FUTURE does: ON-RESOLVE runs (and its return value resolves the new future) if FUTURE resolves; ON-REJECT runs if FUTURE rejects.  Whichever callback is omitted passes FUTURE’s own state/value through unchanged.  If FUTURE has already settled, the callback runs immediately and the new future settles synchronously; otherwise it runs later, when FUTURE settles.

*** [[file:../sprite-future.el::137][sprite-future-wait]]
:PROPERTIES:
:CUSTOM_ID: sprite-future-wait
:END:

: (fn FUTURE &key TIMEOUT)

Block until /FUTURE/ settles; return its value or nil on timeout/rejection. Drives process output while waiting, mirroring ~sprite-direct-promise-wait~. /TIMEOUT/ overrides ~sprite-direct-blocking-timeout~; nil waits indefinitely.

*** [[file:../sprite-future.el::202][sprite-future-eval]]
:PROPERTIES:
:CUSTOM_ID: sprite-future-eval
:END:

Evaluate FORM in sprite TARGET asynchronously; return a ‘sprite-future’. Dispatches on ~sprite-communication-backend~, the same way ~sprite--call-and-read~ does for the synchronous API.

*** TODO [[file:../sprite-future.el::222][sprite-future-rejected]]
:PROPERTIES:
:CUSTOM_ID: sprite-future-rejected
:END:

*no docstring*

*** [[file:../sprite-future.el::229][sprite-future-async-defun]]
:PROPERTIES:
:CUSTOM_ID: sprite-future-async-defun
:END:

: (fn NAME ARGLIST &rest BODY)

Define /NAME/ as an async sprite workflow. Inside /BODY/, ~sprite-future-await~ suspends the generator until a future settles, without blocking the parent Emacs event loop.  Calling /NAME/ returns immediately with a ‘sprite-future’ for the workflow’s overall result.

*** [[file:../sprite-future.el::242][sprite-future-await]]
:PROPERTIES:
:CUSTOM_ID: sprite-future-await
:END:

Inside a ~sprite-future-async-defun~ body, suspend until FUTURE settles. Resumes with FUTURE’s resolved value when it resolves.  When FUTURE rejects, signals ‘sprite-future-rejected’ with the rejection value at this point, so a ~condition-case~ wrapping ~sprite-future-await~ can catch it.

** sprite-heartbeat

*** [[file:../sprite-heartbeat.el::31][sprite-heartbeat-defhandler]]
:PROPERTIES:
:CUSTOM_ID: sprite-heartbeat-defhandler
:END:

Define a function NAME callable by sprites. ARGS must begin with a symbol that receives the calling sprite’s full name; ‘last-contact’ is updated for that sprite before BODY runs.

Example: (sprite-heartbeat-defhandler handle-status (caller status) (message "%s reported: %s" caller status))

*** [[file:../sprite-heartbeat.el::48][sprite-heartbeat-parent-socket-name]]
:PROPERTIES:
:CUSTOM_ID: sprite-heartbeat-parent-socket-name
:END:

Return the socket name of this sprite’s parent instance. Derived from the leading component of ~sprite-instance-name~. Falls back to the instance name itself when called on the root instance.

*** [[file:../sprite-heartbeat.el::56][sprite-heartbeat-send]]
:PROPERTIES:
:CUSTOM_ID: sprite-heartbeat-send
:END:

Evaluate FORM in the parent instance, tagging this sprite as the sender. The parent records a heartbeat for this sprite before evaluating FORM. Returns the result of FORM as read from the parent.

** sprite-list

*** [[file:../sprite-list.el::98][sprite-list-mode-map]] :variable:
:PROPERTIES:
:CUSTOM_ID: sprite-list-mode-map
:END:

Keymap for ~sprite-list-mode~.

*** TODO [[file:../sprite-list.el::101][map]]
:PROPERTIES:
:CUSTOM_ID: map
:END:

*no docstring*

*** TODO [[file:../sprite-list.el::102][map]]
:PROPERTIES:
:CUSTOM_ID: map
:END:

*no docstring*

*** TODO [[file:../sprite-list.el::103][map]]
:PROPERTIES:
:CUSTOM_ID: map
:END:

*no docstring*

*** TODO [[file:../sprite-list.el::104][map]]
:PROPERTIES:
:CUSTOM_ID: map
:END:

*no docstring*

*** TODO [[file:../sprite-list.el::105][map]]
:PROPERTIES:
:CUSTOM_ID: map
:END:

*no docstring*

*** TODO [[file:../sprite-list.el::106][map]]
:PROPERTIES:
:CUSTOM_ID: map
:END:

*no docstring*

*** TODO [[file:../sprite-list.el::107][map]]
:PROPERTIES:
:CUSTOM_ID: map
:END:

*no docstring*

*** TODO [[file:../sprite-list.el::108][map]]
:PROPERTIES:
:CUSTOM_ID: map
:END:

*no docstring*

*** TODO [[file:../sprite-list.el::109][map]]
:PROPERTIES:
:CUSTOM_ID: map
:END:

*no docstring*

*** TODO [[file:../sprite-list.el::110][map]]
:PROPERTIES:
:CUSTOM_ID: map
:END:

*no docstring*

*** TODO [[file:../sprite-list.el::111][map]]
:PROPERTIES:
:CUSTOM_ID: map
:END:

*no docstring*

*** [[file:../sprite-list.el::115][sprite-list-mode]] :command:
:PROPERTIES:
:CUSTOM_ID: sprite-list-mode
:END:

Major mode for viewing and managing sprite daemons.

Key             Binding ------------------------------------------------------------------------------- TAB		forward-button SPC		scroll-up-command -		negative-argument 0 .. 9		digit-argument <		beginning-of-buffer >		end-of-buffer ?		sprite-list-menu S		tabulated-list-sort c		sprite-list-create d		sprite-list-decommission f		sprite-list-open-frame g		sprite-list-refresh h		describe-mode i		sprite-list-info m		sprite-list-menu n		next-line o		sprite-list-open-log p		sprite-list-check q		quit-window r		sprite-list-restart s		sprite-list-stop {		tabulated-list-narrow-current-column }		tabulated-list-widen-current-column DEL		scroll-down-command S-SPC		scroll-down-command M-<left>	tabulated-list-previous-column M-<right>	tabulated-list-next-column <backtab>	backward-button <follow-link>	mouse-face <mouse-2>	mouse-select-window

C-M-i		backward-button

In addition to any hooks its parent mode ~tabulated-list-mode~ might have run, this mode runs the hook ~sprite-list-mode-hook~, as the final or penultimate step during initialization.

*** [[file:../sprite-list.el::131][sprite-list-refresh]] :command:
:PROPERTIES:
:CUSTOM_ID: sprite-list-refresh
:END:

Refresh the sprite overview buffer.

*** [[file:../sprite-list.el::146][sprite-list-info-map]] :variable:
:PROPERTIES:
:CUSTOM_ID: sprite-list-info-map
:END:

Keymap for sprite info help buffers. Add context-specific bindings here.

*** [[file:../sprite-list.el::153][sprite-list-info]] :command:
:PROPERTIES:
:CUSTOM_ID: sprite-list-info
:END:

Show a help-window detail buffer for the sprite at point.

*** [[file:../sprite-list.el::183][sprite-list]] :command:
:PROPERTIES:
:CUSTOM_ID: sprite-list
:END:

Open the sprite overview buffer.

*** [[file:../sprite-list.el::222][sprite-list-create]] :command:
:PROPERTIES:
:CUSTOM_ID: sprite-list-create
:END:

Create a new sprite from the overview buffer.

*** [[file:../sprite-list.el::228][sprite-list-decommission]] :command:
:PROPERTIES:
:CUSTOM_ID: sprite-list-decommission
:END:

Decommission the sprite at point.

*** [[file:../sprite-list.el::236][sprite-list-restart]] :command:
:PROPERTIES:
:CUSTOM_ID: sprite-list-restart
:END:

Restart the sprite at point.

*** [[file:../sprite-list.el::242][sprite-list-stop]] :command:
:PROPERTIES:
:CUSTOM_ID: sprite-list-stop
:END:

Stop the sprite at point.

*** [[file:../sprite-list.el::248][sprite-list-open-log]] :command:
:PROPERTIES:
:CUSTOM_ID: sprite-list-open-log
:END:

Open the communication log for the sprite at point.

*** [[file:../sprite-list.el::253][sprite-list-check]] :command:
:PROPERTIES:
:CUSTOM_ID: sprite-list-check
:END:

Verify whether the sprite at point is reachable via emacsclient. Updates the sprite’s running status and refreshes the list.

*** [[file:../sprite-list.el::264][sprite-list-open-frame]] :command:
:PROPERTIES:
:CUSTOM_ID: sprite-list-open-frame
:END:

Open a new Emacs frame connected to the sprite at point.

*** [[file:../sprite-list.el::270][sprite-list-menu]] :command:
:PROPERTIES:
:CUSTOM_ID: sprite-list-menu
:END:

Actions for the sprite overview buffer.

** sprite-session

*** [[file:../sprite-session.el::26][sprite-session-idle-timeout]] :option:
:PROPERTIES:
:CUSTOM_ID: sprite-session-idle-timeout
:END:

Seconds of Emacs idle before ~sprite-session-idle-hook~ fires.

*** [[file:../sprite-session.el::31][sprite-session-idle-hook]] :variable:
:PROPERTIES:
:CUSTOM_ID: sprite-session-idle-hook
:END:

Hook run when Emacs has been idle for ~sprite-session-idle-timeout~ seconds. Each function is called with no arguments.

*** [[file:../sprite-session.el::38][sprite-session-start-idle-timer]]
:PROPERTIES:
:CUSTOM_ID: sprite-session-start-idle-timer
:END:

Start a repeating idle timer that runs ~sprite-session-idle-hook~. Cancels any existing timer first.

*** [[file:../sprite-session.el::46][sprite-session-stop-idle-timer]]
:PROPERTIES:
:CUSTOM_ID: sprite-session-stop-idle-timer
:END:

Cancel the idle timer.

*** [[file:../sprite-session.el::52][sprite-session-sync-idle-timer]]
:PROPERTIES:
:CUSTOM_ID: sprite-session-sync-idle-timer
:END:

Start or stop the idle timer to match ~sprite-session-idle-hook~ membership. Starts the timer when the hook is non-nil; stops it when empty. Always logs: sprite-session: idle timer <running|stopped> (<N> registered ops)

*** [[file:../sprite-session.el::67][sprite-session-add-on-idle]]
:PROPERTIES:
:CUSTOM_ID: sprite-session-add-on-idle
:END:

Add FN to ~sprite-session-idle-hook~ and start the timer if needed. Logs the registration and delegates to ~sprite-session-sync-idle-timer~.

*** [[file:../sprite-session.el::76][sprite-session-remove-on-idle]]
:PROPERTIES:
:CUSTOM_ID: sprite-session-remove-on-idle
:END:

Remove FN from ~sprite-session-idle-hook~ and stop the timer if the hook is empty. Logs the deregistration and delegates to ~sprite-session-sync-idle-timer~.

*** [[file:../sprite-session.el::88][sprite-session-before-sleep-hook]] :variable:
:PROPERTIES:
:CUSTOM_ID: sprite-session-before-sleep-hook
:END:

Hook run before the system suspends or hibernates. Each function is called with no arguments.  Only fires on Linux systems with DBus support and systemd-logind.

*** [[file:../sprite-session.el::93][sprite-session-after-sleep-hook]] :variable:
:PROPERTIES:
:CUSTOM_ID: sprite-session-after-sleep-hook
:END:

Hook run when the system resumes from suspend or hibernate. Each function is called with no arguments.  Only fires on Linux systems with DBus support and systemd-logind.

*** [[file:../sprite-session.el::116][sprite-session-start-logind-watch]]
:PROPERTIES:
:CUSTOM_ID: sprite-session-start-logind-watch
:END:

Register DBus signals to run sleep/wake/lock/screensaver hooks. Cancels any existing registrations first to prevent duplicates.

*** [[file:../sprite-session.el::164][sprite-session-stop-logind-watch]]
:PROPERTIES:
:CUSTOM_ID: sprite-session-stop-logind-watch
:END:

Unregister the logind PrepareForSleep DBus signal and other signals.


